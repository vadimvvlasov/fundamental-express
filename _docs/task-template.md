# <Title>

## Problem
What is broken, missing, or misleading today — stated concretely, with a
pointer to where it actually lives (file:line, report field, command
output). No solutioning here beyond what's needed to make the problem
legible.

## Acceptance Criteria
Checkable, one at a time — someone should be able to run a command or
open a report and answer yes/no for each line, no judgment calls.

- [ ] ...
- [ ] ...

## Edge Cases Considered
Situations the original report/request did not account for, and what
happens to each one under this task's fix (handled here / explicitly
deferred to Out of Scope / already impossible given existing code).

- ...

## Out of Scope
Anything a reader might reasonably expect this task to also cover, but
that this task deliberately does not do — each with a link to the
follow-up issue tracking it, so nothing is silently dropped.

- ... → see [<follow-up id>](./follow-ups.md#<anchor>)
